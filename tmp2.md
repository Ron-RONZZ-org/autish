# feature enhancements

- add context specific warning to `encik forigi/vorto forigi` if there are references in other entries which will be broken if a given entry is deleted (`ligilo/superklaso`,etc.)

- `encik modifi` should be enhancement to allow
  - passing directly a new `.enc` file to replace existing
  - editing the fields directly by relevant CLI options, similar to in `vorto modifi`
