# .readthedocs.yaml
# Read the Docs configuration for autish

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - requirements: docs/requirements.txt
    - path: .

sphinx:
  configuration: docs/source/conf.py

formats:
  - pdf
  - epub
