# feature enhancements

- the linked nodes in `superklaso` and `ligilo` should also be rendered clikable for `encik vidi`.
- similarly, the `ligilo`j in `vorto` should also be clickable in `vorto vidi`
