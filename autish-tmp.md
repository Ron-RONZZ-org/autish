# optimisations

## vorto

- serci
  - add fuzzy search support
    - in absence of exact results, display closely matching results (small differences in spelling, etc.)
    - can be toggled
      - default on
      - CLI: --preciza -p flag to turn off
      - interactive mode: `{search term}/p`flag
