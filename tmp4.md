COPILOT CLI

# feature enhancement

## `vorto` interactive mode

- should implement full nvim text edition functionality
    - certain navigation functions are still broken
      - e.x., in `vidi` view, `ctrl+rt_arrow|lt_arrow` are not jumping entire words
    - bug in common text editor module ?!
    - or is the common text editor module actually not used ?!
  - fix it!

## `encik serci`

### ranking strategy

- beware that user is unlikely to search for explanatory content between `()`
  - ek., for search term `AI modelo`, should match perfectly for `AI (artefarita inteligenteco) modelo `

### `--limo/-lo`

- change default limit to 20, and `vorto serci` default to 10
## `encik aldoni`

- new option: `-v/--vidi` `--html/-H`
  - show the entry after addition as if `encik vidi` was used
- `--semantika-kopii  -sk` and `--kopii -k` should become compatible with --html/-H


